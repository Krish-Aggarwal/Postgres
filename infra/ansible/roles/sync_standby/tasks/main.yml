---
- name: Stop PostgreSQL on sync standby
  service:
    name: postgresql
    state: stopped

- name: Remove existing data directory
  file:
    path: "{{ postgres_data_dir }}"
    state: absent

- name: Create fresh data directory
  file:
    path: "{{ postgres_data_dir }}"
    state: directory
    owner: postgres
    group: postgres
    mode: '0700'

- name: Run pg_basebackup from primary
  become_user: postgres
  shell: |
    pg_basebackup \
      -h {{ hostvars[groups['primary'][0]]['ansible_host'] }} \
      -D {{ postgres_data_dir }} \
      -U {{ replication_user }} \
      -P \
      -R \
      --wal-method=stream \
      --slot=sync_standby_slot
  environment:
    PGPASSWORD: "{{ replication_password }}"
  register: basebackup_result
  failed_when: basebackup_result.rc != 0

- name: Set application_name for sync standby in postgresql.auto.conf
  lineinfile:
    path: "{{ postgres_data_dir }}/postgresql.auto.conf"
    line: "application_name = 'sync_standby_az_b'"
    state: present

- name: Set correct ownership on data directory
  file:
    path: "{{ postgres_data_dir }}"
    owner: postgres
    group: postgres
    recurse: yes

- name: Start PostgreSQL on sync standby
  service:
    name: postgresql
    state: started
    enabled: yes

- name: Verify sync standby is in recovery mode
  become_user: postgres
  shell: psql -c "SELECT pg_is_in_recovery();"
  register: recovery_check
  failed_when: "'t' not in recovery_check.stdout"
  changed_when: false

- name: Show recovery status
  debug:
    msg: "{{ recovery_check.stdout }}"